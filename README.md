2026-04-15
파이썬 수업 시작


```
1. 타입: string , number , boolean
2. 문자열 표현: " " , ' ', """ """
3. 이스케이프 문자: \\ , \" , \' , \n , \t
4. 문자열 길이: len( 자료 )
5. 인덱싱: 문자열[ 인덱스 ]
6. 슬라이싱: 문자열[ 시작인덱스 : 끝인덱스-1 ]
7. 숫자 종류: int , float
8. 사칙 연산자: + , * , - , / , // , % , **
9. 변수 (생략)
10. 복합 대입 연산자: += , *=, -= , /= , %=, **=
11. 출력: print( )
12. 입력: input( )
13. 타입변환: int( ) , float( ) , str( )

13. 문자열 포멧:
    "문자열{}문자열".format(표현식) ,
    f"문자열{표현식} 문자열"

14. 문자열 함수: 
    .upper() , .lower() , .strip() ,
    .find() , 찾을문자 in 문자열 , .split()

15. 비교 연산자: == != > < >= <=

16. 논리 연산자: not and or

17. 조건문: if elif else

18. 리스트: [값 , 값 , 값]
    .append() ,
    .insert() ,
    .pop() ,
    .clear() ,
    .sort() , 
    찾을값 in [리스트] ,
    .len( [리스트] )
    .reversed( [리스트] )
    .enumerate( [리스트] ) : 리스트랑 인덱스 한 번에 반환. 튜플로 반환!
    *[리스트] : '전개연산자'

19. 딕셔너리: { "키": 값 , "키": 값 }
    딕셔너리명[새로운키] = 새로운값
    del 딕셔너리명[키]
    찾을키 in {딕셔너리}
    .get( 키 )
    .items() : 키랑 값을 반복문에서 반환해줌

20. 튜플: ( 값, 값, 값 ) 
    : 괄호 생략 가능 , 수정 불가능

21. 반복문
    (1) for element in [리스트]:
    (2) for key in {딕셔너리}:
    (3) for i in range( 시작값 , 끝값+1 , 증감 ):
    (4) while 조건식:
    (5) break , continue
    (6) [ 표현식 for 반복변수 in 반복할것 if 조건식 ] ('리스트 내포'라고 한대)

22. 여러 줄 문자열: """ """ , .format() , \n , ( 문자열 문자열 )

23. 문자열 합치기: 
    "연결문자열".join( [리스트] ) 
    : 리스트 사이사이에 해당하는 문자열을 연결해준다. (p.263) .csv같은 거 만들 때

24. 함수
    def 함수명(매개변수 , 매개변수):
        실행문
        return 반환값

25. 가변 매개변수: *매개변수
    기본 매개변수: 매개변수 = 기본값

26. 재귀함수: 현재 함수 안에서 자신의 함수를 호출하는 것 (자기 함수 호출)
```